export const ACCESS_TOKEN_TTL_SECONDS = 15 * 60;
export const REFRESH_TOKEN_TTL_SECONDS = 7 * 24 * 60 * 60;

// Окно, в котором проигравший гонку refresh (две вкладки одного браузера дедуплицируют запрос
// сами по себе только внутри одной вкладки — не между ними) трактуется как гонка, а не реюз
// украденного токена (R6, round-2 review). Кука refreshToken общая на весь браузер (не per-tab),
// поэтому реальная гонка ограничена сетевым RTT между отправкой запросов с двух вкладок и записью
// Set-Cookie от первого ответа — не секунды. Сознательно узкое окно (не изначальные 10с): grace
// по fingerprint (IP+UA, легко совпадающему в общей сети/за NAT и подделываемому по UA) — это
// исключение из защиты от реюза украденного токена, чем шире окно, тем дольше атакующий с
// украденным токеном может тихо получать 401 вместо массового отзыва, если совпал с чьим-то
// fingerprint (найдено security-review + /code-review high на этом же батче, независимо).
export const REFRESH_RACE_GRACE_MS = 2_000;
