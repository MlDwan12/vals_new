// Единый фиксированный ключ advisory-лока (не привязан к конкретным строкам) — сериализует саму
// операцию проверки "останется ли хотя бы один активный пользователь с системной ролью", а не
// набор строк, который в момент проверки может параллельно меняться (EXPANSION_TASKS.md §1.5,
// security-audit-2026-08-31.md HIGH №2). Общий для UsersRepository (отключение/удаление/смена
// роли/срок доступа последнего активного системного пользователя) и RolesRepository (снятие
// is_system с последней системной роли) — один и тот же ключ сериализует оба пути между собой,
// иначе гонка "снять флаг у роли" || "деактивировать последнего пользователя другой системной
// роли" осталась бы незакрытой при двух разных ключах.
export const SYSTEM_ROLE_HEADCOUNT_LOCK_KEY = 'system-role-headcount';
