# Бэклог находок

Правило 3 из `FULLSTACK_PLAN.md`: нашёл постороннее — записал строкой сюда, не чинишь в текущем
срезе. Разбираем отдельными задачами, когда до них дойдёт очередь.

| Дата | Где нашли | Находка |
|---|---|---|
| 2026-09-10 | срез 1.1 | `front`: поиск не ограничивает длину запроса сверху, бек требует `Length(2, 200)` (`backend_v2/.../search-query.dto.ts:9`). Запрос длиннее 200 символов даёт 400 → в UI «Что-то пошло не так». |
| 2026-09-10 | срез 1.1 | `front`: нет тестового раннера вообще (в `package.json` только `dev`/`build`/`start`/`lint`). Приёмка срезов на сайте пока только глазами. Этап 0.5 плана закладывает в CI только `tsc --noEmit` и `next build`. |
| 2026-09-10 | срезы 1.2 / 1.3a | `front`: `HOST` (`shared/config/config.ts:5`) после удаления ручных заголовков `origin`/`x-forwarded-host` больше нигде не используется. Вместе с ним подвисает `NEXT_PUBLIC_HOST` в env. Ровно то же в `admin_front` после среза 1.3a: `HOST` в `shared/constants/constants.ts` осиротел. Удалять в обоих репозиториях — отдельной задачей, вдруг понадобится для канонических URL. |
| 2026-09-10 | срез 1.4 | `admin_front`: нет ручной отправки лида в Битрикс. Ручка на беке есть (`POST /admin/client-leads/:id/retry`), а после ETL лиды без `bitrix_lead_id` переносятся со статусом `failed`, и планировщик их намеренно не подхватывает — разбирать придётся руками, а кнопки нет. |
| 2026-09-10 | срез 1.4 | `front`: `/services` и `/articles`/`/cases` при ошибке API отдают пустую страницу с кодом 200, `sitemap.xml` — без единой статьи и кейса. Для поисковика это валидный ответ. Стоит отдавать 500, как теперь делают страницы статьи/кейса/сотрудника после среза 1.1. |
| 2026-09-11 | срез 1.4 | **Решено не чинить.** У 7 исторических заявок на тариф нет снапшота `payload.tariff` — в старой системе его не существовало, ETL копирует `payload` как есть. Блок «Данные по услуге» для них не показывается. Достраивать снапшот из сегодняшних тарифов не стали: цены изменились, это была бы подделка истории. `tariffId`/`periodId` в payload лежат, при необходимости смотреть в Битриксе. |
| 2026-09-11 | этап 0.5 | **Отложено осознанно.** CI есть только в `backend_v2` (`.github/workflows/ci.yml`); в `front` и `admin_front` нет. Заводить после выкатки на прод, а не до — решение пользователя. Три репозитория всё равно переезжают в монорепозиторий, где раскладка будет другая. |
| 2026-09-11 | этап 2 | Ветки: этап 1 лежит в `front/feature/fsd` (130 коммитов впереди `main`), `admin_front/feature/editor-ui-polish` (43), `backend_v2/dev` (4 впереди `master`). Имена к миграции отношения не имеют, правило «один срез — одна ветка» уже нарушено. Разобрать перед выкаткой. |
| 2026-09-11 | срез A.1 | `backend_v2`: `/dashboard/stats` теперь открыт любому вошедшему (легаси-`@Roles(...ALL_ROLES)` отдавал 403 держателю роли, заведённой из панели, — то есть на первом же экране). Побочно: контентная роль видит и агрегаты по клиентам/заявкам (только числа). Если решим прятать — отдавать блоки по правам (`clients.read` и т.п.); сейчас сознательно не делаем. |
| 2026-09-11 | срез A.1 | **Сделано в A.4.** `backend_v2`: три легаси-ручки создания пользователя (`/admin/users/content-managers|client-managers|admins`) остались на `@Roles()` — `createWithRole()` не проверяет actor'а, перевод на `users.manage` без этой проверки дал бы держателю права завести себе пользователя старшей роли. Убрать вместе с переводом админки на `POST /admin/users` в срезе A.4. |
| 2026-09-11 | срез B.1 | `admin_front`: редактор вшивает в контент материала **абсолютный** адрес картинки (`RichTextEditor.tsx:87`, `mediaSrc(url)`). Значение origin берётся из окружения сборки, поэтому статья, отредактированная локально, уносит в контент `http://localhost:4100/...`, а на проде это уже битая картинка. Хранить надо путь от корня (`/uploads/media/<file>`) — сайт раздаёт его со своего домена. Правка на два шага: поменять вставку и разово переписать уже сохранённые ссылки в `content`/`content_html` (скрипт по образцу `migrate-prod-data`). |
| 2026-09-11 | срез B.2 | Потолок веса картинки разный на двух концах: админка режет на 200 КБ (`features/media/create/model/media.schema.ts`), бек пускает до 1 МБ (`media-size-budget.util.ts`). Сейчас строже клиент, поэтому «бек отказал» человек не увидит — но цифра одна и та же должна жить в одном месте. Решить, какая правильная, и свести. |
| 2026-09-11 | срез G.1 | `backend_v2`: журнал забит техническим шумом обновления токена. Пара «401 на любую ручку + `CREATE` в разделе `auth` без пользователя» — это протухший access-токен и автоматический `POST /auth/refresh`, а не отказ в правах и не действие человека. На 138 записей таких пар больше десятка, и в разделе «Вход в панель» они выглядят как чужие неудачные входы. Вдобавок у такой записи `user_id` заполнен, а `username` — нет (25 записей у пользователя 5, из них часть без логина), поэтому в журнале она читается как ничья: админка показывает её как «сотрудник #5». Варианты: не писать в журнал успешный `/auth/refresh` вовсе, либо давать ему отдельное действие (`TOKEN_REFRESH`), чтобы отличался от `LOGIN` и от отказа. |
| 2026-09-11 | срез X.0 | **Сделано в X.3.** `admin_front`: редактор и генератор HTML подключали `Link` дважды — StarterKit 3.x уже содержит его, и рядом стоит отдельный `Link.configure(...)`. tiptap пишет `Duplicate extension names found: ['link']`. На беке это решено через `StarterKit.configure({ link: {...} })` — результат побайтово тот же, предупреждение уходит. В админке сделано так же: генератор удалён целиком вместе с `@tiptap/html`, в редакторе конфиг `Link` переехал внутрь `StarterKit.configure`. |
| 2026-09-11 | срез X.1 | Окружение: у контейнера `vals_new_api` висит **анонимный docker-volume на `/app/node_modules`**. *Уточнено в E.1: он не наследие, а объявлен явно — `backend_v2/docker-compose.yml:58`, строка `- /app/node_modules` в блоке `api`.* Он переживает `docker compose up -d --force-recreate` и перекрывает `node_modules` из образа: после добавления зависимостей образ собран правильно, а контейнер падает с «Cannot find module». Лечится `--renew-anon-volumes`. Обычно такая строка защищает `node_modules` образа от монтирования всего проекта в `/app`, но здесь монтируются только `./src`, `./test` и `./uploads` — перекрывать нечему, и строка выглядит лишней. Проверить и убрать вместе с пересозданием контейнера. |
